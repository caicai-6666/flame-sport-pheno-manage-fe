# 燃动现象管理前端智能体协作规范

本文件是所有智能体参与本项目开发前的必读文件，适用于本仓库及其全部子目录。执行具体任务时，还应继续读取目标目录中层级更深的 `AGENTS.md`（如后续存在）；更深层文件只补充或收紧其目录范围内的规则。

## 1. 开始任务前必须阅读

开始分析、设计或修改代码前，按以下顺序完成上下文检查：

1. 完整阅读本文件，确认当前项目开发约定。
2. 阅读 `description/README.md`，通过文档地图定位当前任务需要的上下文。
3. 阅读 `description/project.md`，了解项目背景、业务目标、角色和核心流程。
4. 阅读 `description/document-style.md`，了解统一的文档结构与写作风格。
5. 阅读与任务相关的架构、功能、组件和接口文档。
6. 根据任务涉及的业务实体，阅读 `description/db/` 下对应的数据库文档；涉及多个实体时，应一并阅读其关联表文档。
7. 检查当前代码、配置、依赖和已有文档，以仓库中的实际状态为准，不凭经验假设项目能力。

如果文档地图中标记的文档尚不存在，不得虚构其中的规则，也不应因此阻塞普通开发。智能体应明确说明文件缺失，并依据本文件、现有代码和已有文档继续工作；只有任务明确要求或当前开发必须同步交付时才创建对应文档。

## 2. 当前项目基线

- 本仓库是燃动现象管理端前端项目。
- 当前技术基线为 Vue 3、Vite 和 JavaScript，使用 ES Modules；页面局部视觉效果可使用已安装的 PixiJS 与 Three.js，Three.js 仅应按需加载，避免影响主页面首屏包体积。
- 以 `package.json` 中的依赖和脚本作为实际开发、构建命令的依据。
- 当前常用命令为：`npm run dev`、`npm run build`、`npm run preview`。
- 客户端前端、后端接口及数据库已有既定设计。管理端开发应优先适配现有业务模型和接口契约，不擅自扩大改动范围。

项目技术栈、目录或脚本发生变化后，应同步更新本节，避免说明与代码脱节。

## 3. 架构与编码原则

### 3.1 高内聚、低耦合

- 页面负责业务编排，组件负责独立且清晰的展示或交互职责。
- 可复用逻辑应按职责沉淀到合适的组件、组合式函数、服务或工具模块中，避免页面文件持续膨胀。
- 组件之间优先通过明确的 `props`、事件或稳定的状态接口协作，避免跨层访问内部实现。
- 网络请求、数据转换、视图状态和展示组件应合理分层，避免互相混杂。
- 不复制已有能力；实现前先搜索项目中是否存在可复用代码。

### 3.2 合理使用设计模式

- 鼓励使用设计模式解决重复变化、状态流转、对象创建和模块协作等实际问题。
- 选择模式时应说明它解决的具体问题，并保持实现可读、可维护。
- 不为简单需求堆叠抽象；抽象层必须具有明确职责或现实复用价值。

### 3.3 控制变更范围

- 优先完成满足需求的最小闭环，不进行与任务无关的大规模重构、依赖升级或风格改写。
- 保持与现有代码风格一致；若需引入新约定，应同步更新文档并说明原因。
- 不覆盖或删除无法确认来源的已有改动。
- 不在代码、配置、日志或文档中写入密钥、令牌及其他敏感信息。

## 4. 中文注释规范

必须在代码关键处添加准确、简洁的中文注释，重点说明代码的业务用途和设计原因，而不是复述语法。

以下位置通常需要注释：

- 关键业务规则、状态转换和权限判断。
- 不直观的数据转换、兼容处理和边界条件。
- 组件、组合式函数或服务之间的重要协作约束。
- 为规避后端、浏览器或第三方能力限制而采用的特殊处理。
- 容易被误删、误改，但对业务正确性有重要影响的实现。

简单赋值、显而易见的循环或模板结构无需机械添加注释。代码变更后必须同步校对相关注释，禁止保留失效或误导性说明。

## 5. 组件与接口开发约定

### 5.1 组件

- 组件应职责单一，名称能够表达业务含义。
- 明确定义并约束 `props`、事件、插槽及暴露方法，避免依赖隐式行为。
- 通用组件不得直接耦合具体页面接口；业务组件不得无边界吸收无关功能。
- 异步页面应根据业务需要处理加载、空数据、失败、无权限和重复提交等状态。

### 5.2 接口

- 接口调用应集中在职责明确的 API 或服务模块中，避免散落在展示组件内。
- 请求参数、响应数据和错误信息应在进入页面展示层前完成必要的适配。
- 字段含义和关联关系必须与现有后端契约及 `description/db/` 文档保持一致。
- 不得仅凭数据库结构猜测接口地址、请求方式或响应格式。契约不明确时，应先检查现有实现和项目文档，再明确记录仍待确认的问题。

## 6. 数据库文档保护规则

`description/db/` 是本项目既定数据模型、字段语义和结构约束的依据，默认只读。该目录不承担业务流程、页面行为或产品规则的说明职责。

- 没有明确修改要求时，不得修改、删除或批量格式化其中的文件。
- 前端使用的字段、枚举、类型、默认值和关联关系应优先参照相关数据库文档。
- 业务背景、用户流程和产品规则应参照 `description/project.md` 及 `description/features/` 下的功能文档，不从数据库结构反推业务行为。
- 发现数据库文档、后端接口和现有前端实现不一致时，不得擅自改变数据模型，应记录差异并向用户说明。
- 只有用户明确要求调整数据库设计或数据库文档时，才可修改该目录；修改前应检查所有关联表和受影响功能。

## 7. 文档同步规范

实现或修改组件、接口及重要业务功能时，文档属于交付内容，必须与代码在同一任务中同步完成。

### 7.1 文档存放位置

- 文档地图：`description/README.md`
- 项目说明：`description/project.md`
- 文档规范：`description/document-style.md`
- 架构文档：`description/architecture/<主题>.md`
- 组件文档：`description/components/<业务模块>/<组件名>.md`
- 接口文档：`description/api/<业务模块>/<接口或服务名>.md`
- 跨组件业务功能文档：`description/features/<业务模块>.md`
- 数据库文档：仅在用户明确要求时更新 `description/db/<表名>.md`

对应目录不存在时，应按上述层级创建，禁止将模块文档堆放在 `description/` 根目录。新增、移动或删除文档时，必须同步更新 `description/README.md`。文档名称应稳定、语义清晰，并尽量与代码模块名称一致。

### 7.2 文档内容

所有文档必须遵循 `description/document-style.md`。该文件尚未创建时，暂时使用以下最低要求：

- 使用清晰的 Markdown 标题层级，标题直接表达内容。
- 先说明用途和适用范围，再说明设计、使用方式与注意事项。
- 组件文档至少记录职责、使用方式、`props`、事件、插槽、主要状态和依赖。
- 接口文档至少记录用途、请求方式、路径、参数、响应、错误处理和调用示例。
- 重要业务功能应记录核心流程、业务规则、状态变化及关联组件或接口。
- 内容必须与当前实现一致；修改行为或契约时同步更新原文档，不重复创建近似文档。

## 8. 标准开发流程

### 开发前

1. 完成必读文件检查。
2. 搜索并理解相关代码、文档和数据库实体。
3. 明确需求范围、接口契约、可复用模块和潜在影响。
4. 发现关键信息缺失或规则冲突时，先说明事实和影响；可安全作出局部假设时，应明确记录假设。

### 开发中

1. 按高内聚、低耦合原则组织代码，并保持最小必要改动。
2. 在关键业务逻辑处补充中文注释。
3. 同步维护组件、接口或功能文档。
4. 持续检查错误态、空状态、边界值以及接口异常。

### 完成前

1. 检查代码、注释和文档是否一致。
2. 运行与改动范围匹配的校验；当前项目至少应运行 `npm run build`。若后续增加 lint、类型检查或测试脚本，也应运行相关命令。
3. 确认没有误改 `description/db/`、无关文件或用户已有改动。
4. 向用户说明代码改动、文档位置、验证结果和仍存在的限制或待确认项。

## 9. 规则优先级与冲突处理

- 用户针对当前任务的明确要求优先级最高。
- `description/project.md` 是项目业务背景和范围的主要依据。
- `description/document-style.md` 是文档格式和表达方式的主要依据。
- `description/db/` 是数据库结构和字段含义的主要依据，并受只读规则保护。
- 实际代码和后端接口是运行行为的事实依据。

如果这些来源互相冲突，应先保留现状、记录具体差异并向用户说明，不得静默选择其中一方或擅自扩大修改范围。
