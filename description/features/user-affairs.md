# 用户事务页面

用户事务页面用于集中处理与具体用户行为和结果相关的管理工作。当前阶段已建立页面入口、三类业务导航骨架，以及接入真实查询接口的**赛季结算列表**、**运动记录智能查询**和**兑换记录智能查询**原型。

---

## 页面进入方式

管理员点击顶部“用户事务”后：

1. 顶部胶囊导航中的白色滑块平滑移动到第三项。
2. 当前页面沿水平方向向左离开可视区域。
3. 用户事务页面从右侧进入。
4. 左侧三个分类条目依次淡入，并默认选中“赛季结算”。

数据看板、平台配置与用户事务始终挂载在同一条三页轨道中。离开可视区域的页面使用 `inert` 隔离交互，页面内部的本地原型状态不会因切换而重建。

---

## 当前业务导航

| 分类 | 业务定位 | 当前状态 |
| --- | --- | --- |
| 赛季结算 | 查看当前结算赛季、正式参赛用户并发放积分 | 已接入真实接口 |
| 运动记录 | 使用自然语言查询运动凭证并导出动态结果 | 本地智能查询原型 |
| 兑换记录 | 查看用户商品兑换及奖品发放情况 | 本地智能查询原型 |

点击分类时，左侧选中流光平滑移动到目标项，右侧内容容器保持稳定，不发生尺寸跳变。

赛季结算、运动记录和兑换记录使用三个独立面板容器。非当前面板会被同时隐藏并设为不可交互，避免业务内容在切换后残留到其他分类。

> [!IMPORTANT]
> “赛季结算”以唯一 `status = 2` 的赛季为范围，不表示每上传一条运动记录就即时发放积分。

---

## 赛季结算

管理员进入“赛季结算”后，页面自动获取唯一结算中赛季，再按接口返回的 `season_user_ids` 批量加载正式参赛用户。参赛记录超过 1000 条时，前端按接口上限顺序分批查询。

当前结算赛季工具栏提供“待终审记录”按钮。按钮使用懒加载的 PixiJS 画布，在悬停或键盘聚焦时播放液态色块扩张与流动反馈。点击后通过结算专用接口一次获取当前赛季全部待终审凭证，并让赛季结算卡片翻到背面，展示与“今日待办”一致的终审工作区；返回时反向翻回结算列表。

“待终审记录”左侧提供红色“一键结算”高风险入口。弹窗明确说明确认后会拒绝全部未决凭证，并根据最终进度定分和发放积分；管理员必须逐字输入 `我确认结算{赛季名称}` 才能启用最终按钮。确认后通过 `POST /settlement/complete` 将整季收口交由一个后端事务完成，请求期间锁定弹窗和用户事务导航。成功后先展示自动拒绝、新定分与新发放汇总，关闭弹窗后退出当前结算态；失败时不自动重试。

终审记录通过 `season_user_id` 关联已经加载的结算用户资料和头像，通过 `project_id` 关联该用户的有效项目，不重复请求用户详情。接口虽然没有直接返回 `level_id`，但挑战等级名称受数据库唯一约束，页面会从完整等级目录恢复等级主键，再与 `project_id` 组合加载目标要求。凭证图片仍通过受保护图片中转接口按审核进度分批加载。

结算工作区的通过与拒绝使用 `POST /settlement/final-review`，不调用普通赛季终审入口。服务端成功后记录从当前队列移除，并同步响应中的项目完成进度；由于终审响应不返回 `final_points`，页面随后只刷新对应用户的结算详情，使自动定分结果及时回到结算列表。

列表展示：

- 用户名称与部门辅助信息。
- 用户在该赛季选择的统一挑战等级。
- 每一个已锁定运动项目的完成进度。
- 该用户本赛季的最终积分。
- 终审和积分发放状态。

结算数据加载完成后，列表记录按接口顺序从右侧逐条减速进入。入场延迟设置总上限，避免正式参赛人数较多时离屏记录长时间不可见；系统启用减少动态效果时直接展示全部记录。

状态展示遵循以下规则：

- `final_points = null`：显示“待终审”。
- `final_points != null` 且 `points_issued = false`：显示“发放积分”按钮。
- `points_issued = true`：显示“已发放”。

> [!IMPORTANT]
> 首次点击“发放积分”只进入 3 秒确认态，管理员再次点击同一用户的“确认发放”后才通过 `POST /settlement/issue-points` 提交。页面只在服务端确认积分流水与 `points_issued` 状态成功更新后切换为已发放，不执行乐观更新。

当当前结算赛季的全部用户均已发放后，页面显示“导出 Excel”入口。导出的 `.xlsx` 文件包含用户、部门、挑战等级、逐项运动进度、结算积分和积分状态；仍有待终审或待发放记录时不提供导出入口。

页面覆盖加载、无结算中赛季、多个结算中赛季、无人参赛、接口失败和手动刷新状态。头像沿用管理端受保护图片中转，单张头像失败时保留姓名首字。

---

## 运动记录智能查询

审核员进入“运动记录”后，可以直接描述想查询的内容，例如：

- 查询本赛季所有待终审的运动记录。
- 查看跑步项目中被终审拒绝的记录。
- 按部门汇总本赛季运动记录审核情况。

后台智能体的结果分为“运动记录”与“聚合结果”两类，分别使用 `records` 和 `aggregate` 标识。前端遍历数据列表中每个对象的键，按首次出现顺序合并为动态表头，再展示对应单元格；因此明细查询和聚合查询可以使用不同列结构，不需要为每一种问题预先编写固定表格。

`records` 的每条数据默认携带隐藏的 `image_url`。该字段不生成表头，点击记录行后在独立查看器中展示凭证照片，长图仅在查看器内部滚动。`aggregate` 行不对应唯一凭证，因此没有点击态和照片入口。

每次查询都会生成一条由提示词和结果摘要组成的历史缩略项，并显示结果类型。历史按新到旧排列，最新结果默认展开；开始新查询时，旧结果自动折叠。管理员可以展开任意历史项查看当次动态表格，并单独下载该次查询的 Excel，文件保留对应查询条件、动态表头和全部可导出结果行，不包含隐藏的凭证照片地址。

> [!IMPORTANT]
> 本轮只实现本地响应模拟。正式后台智能体必须通过受控查询能力访问数据，并执行字段白名单、管理员权限、数据脱敏和查询审计，不能直接执行审核员输入的任意 SQL。

响应结构、分页、导出上限、超时和错误格式仍待后端接口契约确认。当前组件不会根据数据库结构猜测接口路径。

---

## 兑换记录智能查询

管理员进入“兑换记录”后，使用与运动记录一致的自然语言检索、历史折叠、动态表格和 Excel 导出交互。当前可以模拟查询：

- 所有兑换明细。
- 待发放或已发放的兑换记录。
- 按商品汇总的兑换次数、积分和发放情况。

兑换明细不带图片元数据，表格行不可点击，也不创建图片查看器。运动与兑换查询由两个独立组件实例管理状态，一侧的提示词和历史记录不会进入另一侧。

> [!WARNING]
> 当前流水编号、兑换时间和发放状态都是本地原型字段。真实数据必须等待兑换订单与发放接口契约，不能仅根据 `point_record` 猜测完整结构。

---

## 数据与接口边界

- 结算赛季来自 `GET /settlement/current`，用户详情来自 `POST /settlement/participants`，待终审查询与提交分别使用 `GET /settlement/pending-final-reviews` 和 `POST /settlement/final-review`，积分发放使用 `POST /settlement/issue-points`，整季收口使用无请求体的 `POST /settlement/complete`。
- 结算参与者详情按 `season_user_id → user_id` 写入工作台用户目录；后续终审记录使用该关系读取姓名、部门和头像地址，不重复请求用户详情。
- 赛季结算结果使用接口返回的 `final_points`，积分是否已计入流水使用 `points_issued`，前端不自行关联流水推导。
- 用户的赛季项目进度来自 `season_user_project.completion_progress`，挑战等级通过 `season_user.level_id` 关联 `project_level`。
- 运动记录以 `proof_record` 为结构参考，终审状态与进度影响遵循 `description/features/proof-review.md`。
- 智能查询结果需要提供 `records` 或 `aggregate` 类型，具体展示字段由数据列表的键决定。
- 运动记录照片使用 `proof_record.image_url`，但在动态表格与 Excel 中作为隐藏元数据处理。
- 商品信息可以关联 `product`，但当前没有独立兑换订单与发放状态数据库文档。
- 积分发放采用后端幂等结果；`issued_now = false` 仍表示已经发放成功，前端不得重复创建或推导积分流水。

> [!WARNING]
> 兑换记录所需的兑换时间、兑换积分快照、收件信息和发放状态均需以后端契约为准。现阶段只提供不会写入数据的本地查询演示。

## 交互与无障碍

- 顶部模块与左侧分类均使用按钮语义。
- 当前分类通过 `aria-current="page"` 标识。
- 隐藏页面使用 `aria-hidden` 与 `inert`，避免键盘焦点进入不可见区域。
- 系统启用减少动态效果时，取消页面与导航的长位移动画。

## 关联组件与文档

- 工作台外壳：`src/components/layout/MainWorkspaceShell.vue`
- 用户事务页：`src/components/user-affairs/UserAffairsPage.vue`
- 赛季结算面板：`src/components/user-affairs/SeasonSettlementPanel.vue`
- 一键结算确认弹窗：`src/components/user-affairs/SettlementFinalizeDialog.vue`
- 赛季结算接口：`src/api/settlement/settlementApi.js`
- 结算用户分批加载：`src/services/settlementParticipantsLoader.js`
- 运动记录智能查询：`src/components/user-affairs/ProofRecordQueryPanel.vue`
- Excel 导出工具：`src/utils/exportSeasonPointDistribution.js`
- 动态 JSON 导出工具：`src/utils/exportDynamicJsonTable.js`
- 通用模块布局：`src/components/layout/WorkspaceModuleLayout.vue`
- 用户事务组件说明：`description/components/user-affairs/UserAffairsPage.md`
- 运动记录终审：`description/features/proof-review.md`
- 商城与发放管理：`description/features/mall-management.md`
